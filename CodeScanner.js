import React from "react";
import { View, Text } from "react-native";

const CodeScanner = () => {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>This will be the QR code scanner!</Text>
    </View>
  );
};

export default CodeScanner;